# 화면 언어 전환 · 2026-09-21

- 목표: 기존 자료 조회용 언어 토글을 앱의 메뉴·설명·폼·상태 문구에도 연결한다. 한국어 기본, 영어·중국어 간체·일본어 지원.
- 구현: `frontend/src/i18n.ts`와 `locales/*`의 명시적인 번역 사전. React 렌더링 단계에서 앱 문구를 번역하고 이름 있는 자리표시자를 사용한다. `App`이 언어를 구독하고 HTML `lang`을 갱신하며, 화면을 다시 마운트하지 않아 선택 장소·펼친 설명·입력값을 보존한다.
- 범위: 홈/오늘/추천/명소 목록·상세/지도/저장 코스의 모바일·데스크톱, 공통 메뉴·점수·수질·조석 설명, 데이터 조회·정보/즐겨찾기/알림/웹캠/AI 보조 화면. 셸 밖 화면에는 펼치는 언어 선택기를 제공한다. 개발용 `#dev`, `#desktop-kit`은 제품 번역 범위에서 제외한다.
- 계약: 제공처의 장소명·주소·관측소·원본 행/JSON과 사용자 입력·기존 대화·저장 코스의 내용은 원문을 보존한다. 코드·단위·KST·장소 ID·취향 태그·검색 범위를 표시 번역으로 변경하지 않는다. 새 추천/여행 안내만 선택 locale을 전송하고 다른 언어의 후보 token을 재사용하지 않는다. 첫 대화 전 인사말은 즉시 바뀐다.
- 동시 변경: 다른 작업에서 강원 지역 API, 필터/페이지네이션, 기본 `region: gangwon`, `locales/regions.ts`와 지도 마커 계산이 추가됐다. 해당 변경을 보존했으며 언어 선택이 지역을 제거하던 이전 동작은 재도입하지 않았다. 이 작업은 커밋·푸시·배포·운영 데이터 변경을 실행하지 않는다.
- 검증 환경: 기존 의존성과 일부 작업 파일이 macOS dataless 상태여서 읽기가 정체됐다. 준비된 소스 snapshot에 lockfile 기준 의존성을 별도 설치해 검증한다. 원본 파일 다운로드만 요청했으며 동기화 설정이나 파일 내용을 임의 복원하지 않았다.
- snapshot/로그: `/var/folders/ns/8yh7k1zn3q9flcsx0msyzfhm0000gn/T/pongdang-ui-language-ofuupnjk/`. 검사용 PostgreSQL 18은 별도 `127.0.0.1:50139/pongdang_test`. 사용자 확인용 `5173/8000/51906`과 분리했다.
- 최종 검증: Node 24.19.0에서 frontend 115 unit, lint, type/build 통과. 전체 browser 134개 통과 후 동시 지역 표시 변경과 홈 활동명 번역을 합친 최종 소스로 언어/지역 회귀 12개 추가 통과. Backend Ruff lint/format 통과. Backend 전체 실행의 1258 passed/2 skipped 후 테스트 환경의 `TRAVEL_ROUTE_PROVIDER=disabled` 때문에 실패한 모의 제공처 테스트 2개를 해당 override 없이 재실행하여 모두 통과했다. 마지막 장소 ID 연결 수정 후 관련 backend 29개도 통과했다. 실제 외부 제공처 호출 없음. Vite의 기존 500 kB 번들 크기 경고는 남아 있다.
- 추가 회귀: `i18n.test.mjs`의 번역 변수 계약·제공처 원문 보존·재시도 오류 언어 전환, `ui-language.spec.ts`의 상세 화면 즉시 전환/선택 장소·펼친 설명 보존/전송 전 입력 보존/데이터 정보 및 새로고침 유지. 기존 `travel-language.spec.ts`는 번역된 화면 라벨에 맞췄다. 기존 독립성 검사의 데이터 구분 문구는 `t` 호출로 검증한다.
- 실제 브라우저: 사용자에게 열려 있던 `#spots?spot_id=9`에서 영어·일본어·중국어 간체의 메뉴/설명/버튼 변경을 확인했다. 장소명·주소는 원문을 유지한다. 마지막에는 영어를 선택하고 점수 설명을 펼쳐 둔 상태로 미리보기를 유지했다.

## 회귀 수정과 완료 상태

- 최초 전체 browser 125 passed/8 failed의 원인 해결: 번역된 JSX의 원래 공백 복구, `GET livecams/preview/places`에 양수 bigint `spot_id` 전달 연결 및 frontend ID 일치 확인, 홈 복귀 재조회 테스트에서 새 지역 목록의 최초 조회 완료 대기. 실패한 기대값을 없애지 않고 원인 수정 후 전체 134개 통과를 확인했다.
- 장소 ID 연결의 별도 backend 회귀는 일치/미존재/잘못된 ID와 제공처 호출 없음까지 검증한다. 홈에 남은 추천은 장소명 원문을 유지하면서 활동 이름을 현재 UI 언어로 표시한다.
- 동시 지역 표시 변경의 `confirmed` 인자는 실제 동적 응답 형태에 맞춰 `Record<string, unknown>`으로 받으며 주소는 런타임 문자열 검사 후 사용한다. 이 타입 수정 후 lint/type/build를 다시 통과했다.
- 2026-09-21 04:39 KST에 현재 작업 폴더와 검사 snapshot의 frontend/src·tests, backend/app·tests 349개 파일이 바이트 단위로 일치함을 확인했다. `final-source-comparison.json`에 기록했다. `git diff --check` 통과.
- 검사용 PostgreSQL 50139는 정상 종료했다. 사용자 확인 서버 5173/8000 및 실제 자료 확인용 DB 51906은 유지했다. 요청한 언어 전환 구현과 검증 완료, 이 작업의 미해결 차단 사항 없음.
