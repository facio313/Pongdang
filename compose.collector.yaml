# Production-only connection to the read-only Multtara data source.
# The application database remains on Pongdang's own default network.
services:
  backend:
    networks:
      - default
      - collector-db

networks:
  collector-db:
    external: true
    name: cksDB-pongdang-explorer
